{
    "name": "${NAME}",
    "image": "${IMAGE}",
    "cpu": ${CPU},
    "memory": ${MEMORY},
    "essential": ${ESSENTIAL},
    "portMappings": [
    {
        "containerPort": ${PORT}
    }
    ],
    "environment": ${ENV_VARS}
}